version: 2

models:
  - name: dim_time_spine
    description: "Calendar date spine required by MetricFlow semantic layer."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: "Daily grain date."
        granularity: day
        data_tests:
          - not_null
          - unique
